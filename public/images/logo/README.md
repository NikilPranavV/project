Drop your logo file here.

- Recommended filenames: `logo.svg`, `logo.png`, or `logo.webp`.
- Replace `logo-placeholder.svg` with your final design.
- For best results use an SVG with a transparent background sized ~400–800px wide.

If you'd like, I can automatically update the header and footer to reference `public/images/logo/logo.svg` once you add it.
